#!/bin/bash
DIR="$(dirname $(realpath $0))"
"$DIR/phpstan-bin/install"
BASEDIR="$DIR/.."
docker run --rm -v "${BASEDIR}":/app -w /app -u $(id -u):$(id -g) php:cli-alpine php bin/phpstan-bin/vendor/bin/phpstan analyse -c phpstan.neon $@ --memory-limit=1G